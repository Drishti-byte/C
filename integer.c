/* program to print an integer entered by the user*/
#include <stdio.h>
int main()
{
    int num;
    printf("Enter a number:");
    scanf("%d",&num);
    printf("Number is %d",num);
    return 0;
}